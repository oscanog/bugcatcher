# BugCatcher Deployment

The canonical production deployment runbook now lives at `infra/DEPLOYMENT.md`.

Use the `infra/` paths for documentation, SQL bootstrap files, nginx config, and shared production config templates.

The root `deploy/*.sh` scripts remain as temporary compatibility wrappers for one release cycle.
